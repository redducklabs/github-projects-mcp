# Core dependencies
mcp[cli]>=1.12.0
gql[requests]>=3.5.0
pydantic>=2.0.0
requests>=2.25.0

# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt